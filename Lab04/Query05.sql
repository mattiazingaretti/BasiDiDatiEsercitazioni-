-- Calcolare le coppie di bevitori che risiedono nello
-- stesso indirizzo, evitando nel risultato sia ogni coppia
-- <x,x,>, sia ogni coppia <x,y> se già compare <y,x>

-- Stessa query esercizio 4...